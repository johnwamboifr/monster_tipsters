const FilterBar = ({ children }) => (
  <div className="flex flex-wrap items-center gap-2 rounded-2xl border border-white/10 bg-slate-950/70 p-3">
    {children}
  </div>
);

export default FilterBar;
